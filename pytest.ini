[pytest]
pythonpath = src
# tests/legacy exercises the quarantined orchestration FSM (see
# src/ai_team/_legacy). It is deliberately excluded from the default run so a
# passing suite reflects the compiled graph only.
testpaths =
    tests/unit
    tests/integration
